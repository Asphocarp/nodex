name: Promote Nightly Release

on:
  workflow_call:
    inputs:
      source_sha: { required: true, type: string }
      tag: { required: true, type: string }
      version: { required: true, type: string }
    secrets:
      landing_github_token: { required: true }

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: release-publish
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with: { fetch-depth: 0, persist-credentials: false, ref: '${{ inputs.source_sha }}' }
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with: { node-version-file: .node-version, cache: pnpm, cache-dependency-path: pnpm-lock.yaml }
      - run: pnpm install --frozen-lockfile
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with: { name: 'nodex-release-bundle-${{ inputs.source_sha }}', path: '${{ runner.temp }}/release-bundle' }
      - name: Publish exact prerelease bytes
        env: { GH_TOKEN: '${{ github.token }}' }
        shell: bash
        run: |
          set -euo pipefail
          bundle="${RUNNER_TEMP}/release-bundle/release-bundle.json"
          test "$(node -p "require('${bundle}').releaseIdentity.channel")" = nightly
          pnpm release:tag -- --repo "${GITHUB_REPOSITORY}" --bundle "${bundle}"
          pnpm release:publish:github -- --repo "${GITHUB_REPOSITORY}" --bundle "${bundle}" --notes "${RUNNER_TEMP}/release-bundle/release-notes.md"
          pnpm release:verify:remote -- --repo "${GITHUB_REPOSITORY}" --bundle "${bundle}"

  feed:
    needs: publish
    runs-on: ubuntu-latest
    environment: landing-production
    concurrency:
      group: release-site-publication
      cancel-in-progress: false
      queue: max
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with: { persist-credentials: false, ref: '${{ inputs.source_sha }}' }
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with: { node-version-file: .node-version, cache: pnpm, cache-dependency-path: pnpm-lock.yaml }
      - run: pnpm install --frozen-lockfile
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with: { name: 'nodex-release-bundle-${{ inputs.source_sha }}', path: '${{ runner.temp }}/release-bundle' }
      - name: Project only the nightly feed subtree
        env: { GH_TOKEN: '${{ secrets.landing_github_token }}' }
        shell: bash
        run: |
          set -euo pipefail
          gh auth setup-git
          target="${RUNNER_TEMP}/target-site"
          git clone https://github.com/NodexApp/NodexApp.github.io.git "${target}"
          pnpm release:project:pages -- --bundle "${RUNNER_TEMP}/release-bundle/release-bundle.json" --existing-site-dir "${target}" --site-dir "${target}"
          git -C "${target}" add updates/nightly
          if git -C "${target}" diff --cached --quiet; then exit 0; fi
          git -C "${target}" config user.name github-actions[bot]
          git -C "${target}" config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git -C "${target}" commit -m "deploy: publish Nodex ${{ inputs.version }} nightly feed"
          git -C "${target}" push origin HEAD
          pnpm release:verify:pages -- --bundle "${RUNNER_TEMP}/release-bundle/release-bundle.json"
